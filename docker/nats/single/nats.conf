# Single NATS server with JetStream (local development).
# Not for production (no TLS / auth).

server_name: nats-single

listen: 0.0.0.0:4222
http: 0.0.0.0:8222

jetstream {
  store_dir: /data
  max_mem_store: 1G
  max_file_store: 10G
}
