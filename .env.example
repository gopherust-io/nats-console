# Copy to .env and fill in secrets before `docker compose up`.
# Never commit a real .env.

ENV=development
AUTH_ENABLED=true
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-generate-a-strong-password
ENCRYPTION_KEY=change-me-to-a-long-random-secret-min-16
SESSION_SECRET=change-me-to-another-long-random-secret

# ---------------------------------------------------------------------------
# LOCAL DEV ONLY — plaintext lab stack (docker compose postgres/nats).
# Do NOT use sslmode=disable, nats://, or http monitoring in production.
# ---------------------------------------------------------------------------
POSTGRES_USER=natsconsol
POSTGRES_PASSWORD=change-me-local-postgres-password
POSTGRES_DB=natsconsol
DATABASE_URL=postgres://natsconsol:change-me-local-postgres-password@postgres:5432/natsconsol?sslmode=disable
NATS_URL=nats://nats:4222
NATS_MONITORING_URL=http://nats:8222

HTTP_ADDR=:8080
PUBLIC_BASE_URL=http://localhost:8080
DEFAULT_CLUSTER_NAME=default
STATIC_DIR=/app/web

# Production example (do not mix with LOCAL DEV ONLY block above):
# ENV=production
# DATABASE_URL=postgres://user:pass@db.example:5432/natsconsol?sslmode=verify-full
# NATS_URL=tls://nats.example:4222
# NATS_MONITORING_URL=https://nats.example:8222
# NATS_TOKEN=...
# # or NATS_CREDS_FILE=/run/secrets/nats.creds
# NATS_TLS_CA_FILE=/run/secrets/nats-ca.pem
# NATS_TLS_CERT_FILE=/run/secrets/nats-client.pem
# NATS_TLS_KEY_FILE=/run/secrets/nats-client-key.pem
# NATS_TLS_SERVER_NAME=nats.example
# NATS_TLS_INSECURE_SKIP_VERIFY=false

# Optional tuning
# HTTP_READ_TIMEOUT=10s
# HTTP_WRITE_TIMEOUT=30s
# HTTP_IDLE_TIMEOUT=60s
# With HTTP/3: docker compose --profile http3 up --build
# Then set PUBLIC_BASE_URL=https://localhost

# Go runtime profiling (admin-only, on-demand routes)
PPROF_ENABLED=true
PPROF_AUTH_ENABLED=true

# Alert email delivery (optional — emails console users when an alert first opens)
# Set Console Users email to a real address (not *@local). Requires METRICS_SNAPSHOT_ENABLED=true.
#
# --- Mailpit (local catcher): docker compose --profile mail up --build
# UI: http://localhost:8025
# SMTP_ENABLED=true
# SMTP_HOST=mailpit
# SMTP_PORT=1025
# SMTP_TLS=false
# SMTP_FROM=nats-consol@local.test
# SMTP_USERNAME=
# SMTP_PASSWORD=
#
# --- Gmail (real inbox): create an App Password (Google Account → Security → 2FA → App passwords)
# SMTP_ENABLED=true
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_TLS=true
# SMTP_USERNAME=you@gmail.com
# SMTP_PASSWORD=your-16-char-app-password
# SMTP_FROM=you@gmail.com

# AI assistant (Gemini — NATS JetStream context only)
# AI_ENABLED=true
# AI_API_KEY=your-gemini-api-key
# AI_MODEL=gemini-2.5-flash
